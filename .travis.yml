language: python
python:
  - "2.7"
# command to install dependencies
services:
  - docker
before_install:
  - docker build -t cidc:flask .
# install:
#   - pip install -r requirements.txt
# # command to run tests
script:
  - python -m  pytest tests_square.py
